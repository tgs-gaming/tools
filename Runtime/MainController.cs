using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace com.tgs.rocktool
{
    public class MainController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
         //Start here
         //finish here
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
